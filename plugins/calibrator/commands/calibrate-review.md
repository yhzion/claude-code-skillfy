---
name: calibrate review
description: 누적 패턴 확인 및 Skill 승격
---

# /calibrate review

반복된 패턴을 확인하고 Skill로 승격합니다.

## i18n 메시지 참조

모든 사용자 대면 메시지는 `plugins/calibrator/i18n/messages.json`을 참조합니다.
실행 시 `.claude/calibrator/config.json`의 `language` 필드를 읽어 해당 언어 메시지를 사용합니다.

```bash
LANG=$(cat .claude/calibrator/config.json 2>/dev/null | grep '"language"' | sed 's/.*: *"\([^"]*\)".*/\1/')
LANG=${LANG:-en}  # 기본값: 영어
```

## 실행 전 확인
- `.claude/calibrator/patterns.db` 존재 확인
- 없으면 i18n 키 `calibrate.run_init_first` 안내

## 플로우

### Step 1: 승격 대상 조회
```bash
sqlite3 .claude/calibrator/patterns.db "SELECT id, situation, instruction, count FROM patterns WHERE count >= 2 AND promoted = FALSE ORDER BY count DESC;"
```

### Step 2-A: 대상 없음
i18n 키 참조:
- `review.no_patterns_title` - 타이틀
- `review.no_patterns_desc` - 설명

영어 예시:
```
📊 No patterns available for promotion

Patterns need to repeat 2+ times to be promoted to a Skill.
Keep recording with /calibrate.
```

### Step 2-B: 대상 있음
i18n 키 참조:
- `review.promotion_title` - 타이틀
- `review.promotion_select` - 선택 프롬프트

영어 예시:
```
📊 Skill Promotion Candidates (2+ repetitions)

[1] Model creation → include timestamp (3 times)
[2] API endpoint → error handling (2 times)
[3] Component creation → Props type definition (2 times)

Select number(s) to promote (comma-separated for multiple): _
```

### Step 3: Skill 미리보기
i18n 키 참조:
- `review.preview_title` - 미리보기 타이틀
- `review.skill_learned_from` - learned_from 메타데이터 (placeholders: {count}, {first_seen}, {last_seen})
- `review.skill_section_rules` - 규칙 섹션 헤더
- `review.skill_section_applies` - 적용 상황 섹션 헤더
- `review.save` / `review.edit` / `review.skip` - 버튼

선택된 각 패턴에 대해 (영어 예시):
```
📝 Skill Preview: {situation}

---
name: {kebab-case situation}
description: {instruction}. Auto-applied in {situation} situations.
learned_from: calibrator ({count} repetitions, {first_seen} ~ {last_seen})
---

## Rules

{instruction}

## Applies to

- {situation}

---

[Save] [Edit] [Skip]
```

### Step 4: Skill 생성
저장 선택 시:
```bash
# Skill 이름 생성 (kebab-case)
SKILL_NAME=$(echo "{situation}" | tr ' ' '-' | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9-]//g')

# Skill 디렉토리 및 파일 생성
mkdir -p .claude/skills/learned/$SKILL_NAME

cat > .claude/skills/learned/$SKILL_NAME/SKILL.md << 'EOF'
---
name: {SKILL_NAME}
description: {instruction}. Auto-applied in {situation} situations.
learned_from: calibrator ({count} repetitions, {first_seen} ~ {last_seen})
---

## Rules

{instruction}

## Applies to

- {situation}

## Learning History

This Skill was auto-generated by Calibrator.
- First detected: {first_seen}
- Last detected: {last_seen}
- Repetitions: {count}
EOF

# patterns 테이블 업데이트
sqlite3 .claude/calibrator/patterns.db "UPDATE patterns SET promoted = TRUE, skill_path = '.claude/skills/learned/$SKILL_NAME' WHERE id = {id};"
```

### Step 5: 완료
i18n 키 참조:
- `review.complete_title` - 완료 타이틀
- `review.complete_desc` - 완료 설명 (placeholder: {situation})

영어 예시:
```
✅ Skill created

- .claude/skills/learned/{skill-name}/SKILL.md

Claude will now automatically apply this rule in "{situation}" situations.
```

## 참고: Skill 이름 변환 규칙

situation을 kebab-case로 변환:
- 공백 → 하이픈
- 대문자 → 소문자
- 특수문자 제거
- 예: "모델 생성" → "모델-생성"
- 예: "API 엔드포인트" → "api-엔드포인트"
